# Gayadi Design Guide

이 문서는 Gayadi Android 앱에서 이미 합의되고 구현된 디자인을 새 화면에도 일관되게 적용하기 위한 기준이다. 새 페이지를 만들거나 기존 UI를 수정할 때는 디자인을 처음부터 다시 만들지 말고 이 문서와 공통 컴포넌트를 먼저 확인한다.

> 기준 시점: 2026-08-31  
> 구현 환경: Android Jetpack Compose + Material 3  
> 코드 기준점: `core/designsystem`, `core/ui`, `feature/home`

## 1. 제품의 시각 방향

Gayadi는 여러 사람이 함께 여행을 준비하는 과정을 가볍고 친근하게 보여 주는 앱이다.

- 기본 화면은 밝고 정돈된 흰색 또는 매우 옅은 회색 배경을 사용한다.
- 중요한 행동은 짙은 남청색으로 분명하게 표시한다.
- 캐릭터와 여행 일러스트는 친근함을 담당하고, 나머지 UI는 장식을 줄여 정보가 먼저 보이게 한다.
- 제목, 현재 상태, 다음 행동의 위계가 한눈에 보여야 한다.
- 한 화면에 여러 강조색을 경쟁시키지 않는다. 의미가 정해진 태그, 혼잡도, 오류 상태에서만 보조색을 사용한다.
- 그림자보다는 배경색, 1dp 테두리, 간격으로 영역을 구분한다.

## 2. 반드시 먼저 재사용할 코드

새 컴포넌트를 만들기 전에 아래 구현으로 해결할 수 있는지 확인한다.

| 용도 | 구현 위치 |
|---|---|
| 앱 테마, Material 색상 연결 | `core/designsystem/.../theme/Theme.kt` |
| 색상 토큰 | `core/designsystem/.../theme/Color.kt` |
| Pretendard 및 타입 스케일 | `core/designsystem/.../theme/Type.kt` |
| 기본 입력 필드 | `core/designsystem/.../components/GayadiCompactTextField.kt` |
| 뒤로 가기와 화면 제목 | `core/ui/.../components/GayadiTopAppBar.kt` |
| 하단 탭 | `core/ui/.../components/BottomNavBar.kt` |
| 로딩 상태 | `core/ui/.../components/GayadiLoadingScreen.kt` |
| 일정 액션 바텀시트 | `core/ui/.../components/ScheduleOptionsBottomSheet.kt` |
| 첫 사용 기능 안내 | `core/ui/.../components/UsageGuideOverlay.kt` |
| 사용자 캐릭터 아바타 | `core/ui/.../components/UserCharacterAvatar.kt` |
| 여행 성향 결과 UI | `core/ui/.../components/TravelResultDetails.kt` |

경로의 `...`는 `src/main/java/com/gayadi/android/ui`를 뜻한다. 동일한 패턴이 두 화면 이상에서 필요하면 feature 내부에 복사하지 말고 `core/designsystem` 또는 `core/ui`의 공통 컴포넌트로 승격한다.

## 3. 색상

색상은 가능한 한 `Color.kt`의 이름 있는 토큰을 사용한다. 화면 파일에 새 `Color(0x...)` 값을 바로 추가하지 않는다.

### 기본 팔레트

| 역할 | 토큰 | 값 | 사용 |
|---|---|---:|---|
| 주요 액션 | `PrimaryAction` | `#343548` | 주요 버튼, 선택 상태, 진행률, 활성 하단 탭 |
| 브랜드 블루 | `PrimaryBlue` | `#5B9BD5` | 검색 포커스, 가벼운 브랜드 강조 |
| 진한 브랜드 블루 | `PrimaryBlueDark` | `#4A8AC4` | 보조 액션 |
| 앱 배경 | `Background` | `#FFFFFF` | 기본 화면 및 카드 |
| 옅은 표면 | `SurfaceLight` | `#F5F7FA` | 입력 영역, 구획 배경 |
| 카드 표면 | `SurfaceCard` | `#F8F9FB` | 보조 카드 |
| 주요 텍스트 | `TextPrimary` | `#1C1C1E` | 제목, 본문, 핵심 수치 |
| 보조 텍스트 | `TextSecondary` | `#8E8E93` | 설명, 메타데이터, 비활성 탭 |
| 3차 텍스트 | `TextTertiary` | `#AEAEB2` | 힌트와 우선순위가 낮은 정보 |
| 구분선 | `Divider` | `#E5E5EA` | 섹션 구분선 |
| 테두리 | `Border` | `#D1D1D6` | 입력창 및 카드 경계 |

### 의미 색상

- 태그는 `TagPink`, `TagGreen`, `TagBlue`, `TagOrange`, `TagRed`, `TagPurple`, `TagGray`와 각각의 `...Text` 쌍을 사용한다.
- 혼잡도는 `CrowdedLow`, `CrowdedMedium`, `CrowdedHigh`를 사용한다.
- 안내 배너는 `AlertBlue` 배경과 `AlertBlueText` 텍스트를 함께 사용한다.
- 오류는 `MaterialTheme.colorScheme.error` 또는 의미가 같은 공통 오류 토큰을 사용한다.
- 의미 색상은 장식용으로 사용하지 않는다.

### 홈 전용 분위기

우리여행 홈은 여행의 설렘을 표현하기 위해 위에서 아래로 `#F2FAFF → #F8F8FA → #FFF7F0`의 매우 옅은 그라데이션을 사용한다. 이 그라데이션은 홈과 같이 감성적 진입점에만 사용하며 폼, 설정, 상세 화면의 기본 배경으로 확장하지 않는다.

## 4. 타이포그래피

모든 텍스트는 `PretendardFontFamily`와 `GayadiTypography`를 기준으로 한다. 특별한 로고 이미지가 아닌 일반 텍스트에 다른 서체를 추가하지 않는다.

| Material 스타일 | 크기 / 행간 | 굵기 | 권장 용도 |
|---|---:|---|---|
| `headlineLarge` | 28 / 36sp | Bold | 결과 화면의 핵심 제목 |
| `headlineMedium` | 24 / 32sp | Bold | 큰 화면 제목 |
| `headlineSmall` | 20 / 28sp | SemiBold | 질문, 상세 화면 제목 |
| `titleLarge` | 18 / 26sp | SemiBold | 섹션 제목 |
| `titleMedium` | 16 / 24sp | Medium | 버튼, 카드 제목 |
| `titleSmall` | 14 / 20sp | Medium | 작은 카드 제목 |
| `bodyLarge` | 16 / 24sp | Regular | 주요 본문 |
| `bodyMedium` | 14 / 20sp | Regular | 기본 설명 |
| `bodySmall` | 12 / 16sp | Regular | 메타데이터, 보조 설명 |
| `labelLarge` | 14 / 20sp | SemiBold | 강조 라벨과 칩 |
| `labelMedium` | 12 / 16sp | Medium | 태그와 작은 액션 |
| `labelSmall` | 10 / 14sp | Medium | 매우 작은 상태 라벨 |

현재 화면에서 자주 쓰는 21–22sp 제목은 상단 앱 바 또는 홈의 핵심 메시지에만 허용한다. 새 UI에서는 임의의 `fontSize`보다 `MaterialTheme.typography`를 우선 사용하고, 예외가 필요하면 같은 위계의 기존 화면을 근거로 삼는다.

문장 규칙:

- 사용자 안내는 딱딱한 명사형보다 짧고 친근한 존댓말을 사용한다: `조건에 맞는 장소가 없어요`.
- 버튼은 행동을 명확히 쓴다: `다음`, `수정 완료`, `장소 추가`, `다시 시도`.
- 제목 끝에는 불필요한 마침표를 붙이지 않는다.
- 날짜, 인원, 상태 같은 메타데이터는 주요 제목보다 한 단계 낮은 색과 크기를 사용한다.

## 5. 간격과 레이아웃

4dp 단위 그리드를 기본으로 하고, 아래 값을 우선 사용한다.

| 역할 | 값 |
|---|---:|
| 화면 좌우 여백 | 20dp |
| 큰 화면 구획 | 24–32dp |
| 섹션 내부 간격 | 16–20dp |
| 카드 내부 여백 | 12–16dp |
| 관련 요소 간격 | 8–12dp |
| 아이콘과 라벨 간격 | 6–8dp |
| 미세 간격 | 4dp |
| 구분선 | 1dp |

- 스크롤 콘텐츠와 고정 하단 버튼/내비게이션이 겹치지 않게 한다.
- 상태 바와 내비게이션 바는 `statusBarsPadding()` 및 `navigationBarsPadding()`으로 처리한다.
- 화면 너비가 달라도 좌우 20dp 콘텐츠 축을 유지한다.
- 터치 가능한 요소는 가급적 최소 48dp 영역을 확보한다. 시각 아이콘이 작아도 터치 영역은 줄이지 않는다.
- 긴 목록은 `LazyColumn`, 짧은 폼이나 상세 콘텐츠는 `Column + verticalScroll`을 사용한다.

## 6. 형태와 경계

모서리 값은 컴포넌트 역할에 따라 선택한다.

| 형태 | 권장 값 | 사용 |
|---|---:|---|
| 주요 전체 너비 CTA | 0dp | 설문 `다음`, 저장/완료처럼 화면 흐름을 전진시키는 강한 버튼 |
| 입력창과 일반 카드 | 8–12dp | 검색창, 장소 카드, 폼 그룹 |
| 강조 카드 | 14–16dp | 결과 카드, 요약 카드 |
| 칩과 상태 태그 | 12–18dp 또는 `CircleShape` | 필터, 혼잡도, 선택 상태 |
| 바텀시트 상단 | 28dp | 모달 바텀시트 |
| 아바타와 원형 액션 | `CircleShape` | 캐릭터, 참가자, 원형 아이콘 |

- 일반 카드에는 강한 그림자를 넣지 않는다. `SurfaceCard`, `Border` 1dp 또는 둘 중 하나로 구분한다.
- 한 카드 안에서 서로 다른 radius를 혼용하지 않는다.
- 주요 CTA를 둥근 알약 형태로 임의 변경하지 않는다. 현재 제품의 강한 CTA는 각진 형태다.

## 7. 핵심 컴포넌트 패턴

### 상단 앱 바

- 상세/편집 화면은 `GayadiTopAppBar`를 사용한다.
- 최소 높이 64dp, 제목 21sp Bold, 좌측 뒤로 가기, 우측 선택 액션 구조다.
- 목록과 본문 경계가 필요할 때만 divider를 표시한다.
- 홈처럼 자체 히어로 영역이 있는 화면에는 앱 바를 억지로 추가하지 않는다.

### 버튼

- 한 화면의 주요 액션은 원칙적으로 하나다.
- 주요 액션: `PrimaryAction` 배경 + 흰색 텍스트.
- 보조 액션: 흰색 또는 옅은 표면 + `PrimaryAction` 텍스트/테두리.
- 비활성 상태는 옅은 회색 배경과 보조 텍스트를 사용하며, 활성 상태와 명확히 구분한다.
- 하단 고정 CTA는 50–55dp 높이를 기준으로 한다.
- 진행 중에는 중복 탭을 막고 로딩 상태를 버튼 또는 화면에 표시한다.

### 카드와 목록

- 카드 안의 위계는 `제목 → 핵심 정보 → 보조 정보/상태` 순서로 둔다.
- 이미지가 있는 목록 카드는 이미지 56–64dp, 텍스트 영역, 우측 상태/액션의 3단 구조를 우선한다.
- 카드 전체가 눌리면 별도의 불필요한 `자세히` 버튼을 추가하지 않는다.
- 목록 간 간격은 12dp를 기본으로 한다.

### 입력과 선택

- 라벨, 입력, 오류 메시지 순서가 항상 같아야 한다.
- placeholder는 `TextTertiary`, 입력값은 `TextPrimary`, 오류는 error 색상을 사용한다.
- 검색 입력은 12dp radius와 옅은 회색 컨테이너를 사용한다.
- 단일 선택은 선택된 카드/칩의 배경과 텍스트 대비를 동시에 바꾼다. 색상만으로 상태를 전달하지 않는다.

### 칩과 태그

- 칩 텍스트는 11–12sp, 좌우 10–12dp, 상하 4–7dp를 기준으로 한다.
- 선택 필터는 `PrimaryBlue` 배경 + 흰색 텍스트를 사용할 수 있다.
- 상태 태그는 의미 팔레트의 연한 배경 + 진한 텍스트 쌍을 사용한다.

### 하단 내비게이션

- `BottomNavBar`를 재사용한다.
- 활성 색상은 `PrimaryAction`, 비활성 색상은 `TextSecondary`, 선택 indicator는 투명이다.
- 아이콘은 28dp이고 아이콘 아래에 항상 텍스트 라벨을 둔다.
- 기능 흐름에 따라 `우리여행 / 소비 / 나의여행 또는 마이` 구성을 사용하며, 화면마다 새 탭 스타일을 만들지 않는다.

### 상태 화면

모든 비동기 화면은 다음 네 상태를 설계한다.

1. 로딩: 가능하면 `GayadiLoadingScreen`을 사용한다.
2. 빈 상태: 무엇이 비었는지 설명하고 가능한 다음 행동을 제공한다.
3. 오류: 친근한 설명과 `다시 시도` 같은 복구 행동을 제공한다.
4. 완료: 결과 또는 다음 단계가 즉시 보이게 한다.

## 8. 캐릭터, 이미지, 아이콘

- 가나디 캐릭터는 온보딩, 로딩, 설문, 완료, 빈 상태처럼 감정적 피드백이 필요한 지점에 사용한다.
- 정보 밀도가 높은 폼과 설정 목록에 캐릭터를 장식으로 반복 배치하지 않는다.
- 사용자 캐릭터는 `UserCharacterAvatar`를 사용하고 임의 crop/placeholder 로직을 만들지 않는다.
- 장소 이미지는 `ContentScale.Crop`, 설명 일러스트는 보통 `ContentScale.Fit`을 사용한다.
- 기능 아이콘은 Material Icons 또는 기존 drawable을 우선한다. 같은 의미에 서로 다른 아이콘을 쓰지 않는다.
- 의미 없는 장식 이미지는 접근성 설명을 `null`로 두고, 행동/정보 이미지에는 한국어 설명을 제공한다.

## 9. 모션과 피드백

- 모션은 상태 변화를 이해시키는 용도로만 사용한다.
- 진행률 변화는 약 450ms처럼 짧고 부드럽게 처리한다.
- 로딩 애니메이션이 너무 짧게 깜빡이지 않도록 기존 최소 노출 로직을 재사용한다.
- 탭과 버튼은 즉시 시각 피드백을 주고, 긴 작업은 로딩 또는 비활성 상태를 표시한다.
- 장식적인 반복 애니메이션을 일반 목록이나 폼에 추가하지 않는다.

### 첫 사용 기능 안내

- 첫 사용 안내는 별도 온보딩 화면보다 실제 화면 위의 `UsageGuideOverlay`를 우선 사용한다.
- 안내 문구와 화살표는 대상 요소 가까이에 배치하고, 강조 영역은 실제 터치 영역 크기에 맞춘다.
- 한 화면에서는 핵심 행동만 간결하게 안내하며 기능 목록을 길게 나열하지 않는다.
- 사용자가 강조된 요소를 누르면 원래 동작을 실행하고, 닫기 버튼을 누르면 안내를 종료한다.
- 반복 노출 여부는 안내별 버전 키로 관리한다.

## 10. 접근성과 품질

- 본문 및 주요 액션은 배경과 충분한 명도 대비를 유지한다.
- 아이콘 단독 버튼에는 구체적인 `contentDescription`을 제공한다.
- 선택, 오류, 진행 상태를 색상 하나에만 의존하지 않는다.
- 동적 텍스트가 커져도 핵심 콘텐츠가 잘리거나 버튼과 겹치지 않아야 한다.
- Preview와 UI 테스트에서는 최소한 기본, 긴 텍스트, 빈 상태, 오류 상태를 확인한다.
- 실제 기기 또는 에뮬레이터에서 작은 화면과 일반 화면을 각각 확인한다.

## 11. 새 페이지 작업 절차

새 페이지를 구현할 때 아래 순서를 따른다.

1. 이 문서를 읽고 화면 유형과 가장 가까운 기존 화면을 고른다.
2. 재사용할 공통 컴포넌트와 테마 토큰을 먼저 목록화한다.
3. `제목 → 핵심 정보 → 주요 행동` 위계를 정하고 20dp 콘텐츠 축에 배치한다.
4. 로딩, 빈 상태, 오류, 완료 상태를 함께 구현한다.
5. 새 색상, 폰트 크기, radius를 만들기 전에 기존 토큰으로 해결 가능한지 확인한다.
6. 반복될 UI는 공통 컴포넌트로 추출한다.
7. Preview/UI 테스트와 에뮬레이터 스크린샷으로 간격, 잘림, 시스템 바, 하단 영역을 검증한다.
8. 새로운 디자인 결정이 실제로 추가됐다면 같은 PR에서 이 문서를 갱신한다.

## 12. 구현 완료 체크리스트

- [ ] `GayadiTheme` 안에서 렌더링된다.
- [ ] 화면 좌우 기본 여백이 20dp 축과 맞는다.
- [ ] 색상과 텍스트 스타일이 기존 토큰을 사용한다.
- [ ] 주요 액션이 하나이며 시각 위계가 분명하다.
- [ ] 공통 앱 바, 하단 탭, 로딩, 아바타를 중복 구현하지 않았다.
- [ ] 로딩, 빈 상태, 오류, 완료 상태가 정의됐다.
- [ ] 터치 영역과 `contentDescription`을 확인했다.
- [ ] 작은 화면, 긴 텍스트, 키보드 노출 시 겹침이 없다.
- [ ] 새 디자인 결정이 있다면 `design.md`에 반영했다.

## 13. 대표 참고 화면

- 홈과 여행 준비 위계: `feature/home/.../RealtimeHomeScreen.kt`
- 여행 진행률과 참가자 카드: `feature/home/.../TravelOverviewCard.kt`
- 설문 진행, 선택 카드, 하단 CTA: `feature/survey/.../SurveyScreen.kt`
- 검색, 필터 칩, 장소 목록: `feature/trip/.../PlaceSearchScreen.kt`
- 폼과 비용 입력: `feature/trip/.../ExpenseEditorScreen.kt`
- 설정형 목록: `feature/mypage/.../SettingsScreen.kt`

대표 화면을 그대로 복사하는 것이 아니라, 해당 화면의 위계와 공통 토큰을 재사용한다. 서로 충돌하는 구현이 있다면 `core/designsystem`의 토큰과 이 문서를 우선하고, 필요한 디자인 결정을 문서에 명시한다.
